import React from "react"
import styled from "@emotion/styled"

const contentArea = (props) => {
  const ContentArea = styled.main`
    padding: 10px;
  `
  return <ContentArea>Logged Out</ContentArea>
}

export default contentArea
